# Диагностика проблемы с напоминаниями

Если напоминание о смене подгузника не пришло за 5 минут, выполните следующие шаги:

## Шаг 1: Проверьте, создалось ли напоминание

Откройте консоль браузера (F12) и выполните:

```javascript
// Замените 1 на ваш family_id (обычно 1)
window.debugReminders.checkScheduledReminders(1)
```

Должно показать:
- Есть ли запланированные напоминания
- Когда они запланированы
- Сколько минут до отправки

**Если напоминаний нет:**
- Проверьте, что после добавления смены подгузника в консоли появились логи:
  - `📅 Планирование напоминания о смене подгузника`
  - `✅ Напоминание о смене подгузника запланировано на ...`

**Если напоминание есть, но время уже прошло:**
- Проверьте, что `reminderProcessor` работает
- Выполните: `window.debugReminders.processRemindersNow()`

## Шаг 2: Проверьте, запущен ли обработчик напоминаний

В консоли браузера должны быть логи:
- `Starting reminder processor...` (при загрузке приложения)
- `Reminder processor started (checking every 60 seconds)` (каждую минуту)

**Если этих логов нет:**
- Перезагрузите страницу
- Убедитесь, что вы авторизованы (reminderProcessor запускается только после авторизации)

## Шаг 3: Проверьте, обрабатываются ли напоминания

В консоли браузера каждую минуту должны появляться логи:
- `✅ Processed X reminders: Y sent, Z failed` (если есть напоминания)
- `ℹ️ No reminders to process` (если напоминаний нет, каждые 5 минут)

**Если логов нет:**
- Проверьте, что приложение открыто (обработчик работает только когда приложение активно)
- Проверьте, нет ли ошибок в консоли

## Шаг 4: Проверьте подписки на push-уведомления

```javascript
// Замените 1 на ваш family_id
window.debugReminders.checkPushSubscriptions(1)
```

**Если подписок нет:**
- Перейдите в Настройки → 🔔 Push-уведомления
- Нажмите "Подписаться на уведомления"
- Разрешите уведомления в браузере

## Шаг 5: Создайте тестовое напоминание для проверки

```javascript
// Создайте напоминание на 1 минуту вперед
window.debugReminders.createTestReminder(1, 'diaper')

// Или создайте напоминание для немедленной отправки
window.debugReminders.createImmediateTestReminder(1, 'diaper')

// Затем обработайте его вручную
window.debugReminders.processRemindersNow()
```

## Шаг 6: Полная диагностика

```javascript
// Замените 1 на ваш family_id
window.debugReminders.testReminderSystem(1)
```

Это проверит:
- Запланированные напоминания
- Активные подписки
- Доступность обработчика

## Возможные проблемы и решения

### Проблема 1: Напоминание не создается

**Причина:** Ошибка при планировании напоминания

**Решение:**
1. Проверьте консоль на наличие ошибок
2. Проверьте, что в настройках указан интервал смены подгузника
3. Проверьте, что после добавления смены подгузника в консоли нет ошибок

### Проблема 2: Напоминание создается, но не отправляется

**Причина:** 
- Обработчик не запущен
- Время напоминания еще не наступило
- Приложение закрыто

**Решение:**
1. Убедитесь, что приложение открыто
2. Проверьте, что `reminderProcessor` запущен (логи в консоли)
3. Проверьте время напоминания: `window.debugReminders.checkScheduledReminders(1)`

### Проблема 3: Напоминание обрабатывается, но уведомление не приходит

**Причина:**
- Нет активных подписок
- Подписка недействительна
- Браузер блокирует уведомления

**Решение:**
1. Проверьте подписки: `window.debugReminders.checkPushSubscriptions(1)`
2. Переподпишитесь на уведомления
3. Проверьте настройки браузера (разрешить уведомления)

### Проблема 4: Время напоминания вычисляется неправильно

**Причина:** Проблема с часовыми поясами или временем

**Решение:**
1. Проверьте, что время события (смены подгузника) правильное
2. Проверьте, что интервал смены подгузника указан правильно
3. Проверьте логи планирования в консоли:
   - `⏰ Время следующей смены: ...`
   - `⏰ Время напоминания: ...`
   - `⏰ До напоминания: X минут`

## Проверка в продакшене

Если проблема в продакшене (Vercel):

1. **Проверьте логи Vercel:**
   - Vercel Dashboard → Deployments → Functions → api/push/process-reminders
   - Нажмите View Logs
   - Проверьте, есть ли ошибки

2. **Проверьте переменные окружения:**
   - Vercel Dashboard → Settings → Environment Variables
   - Убедитесь, что добавлены:
     - `SUPABASE_URL`
     - `SUPABASE_SERVICE_ROLE_KEY`
     - `VAPID_PUBLIC_KEY`
     - `VAPID_PRIVATE_KEY`
     - `VAPID_SUBJECT`

3. **Проверьте, что API endpoint доступен:**
   - Откройте консоль браузера в продакшене
   - Выполните: `window.debugReminders.processRemindersNow()`
   - Проверьте, нет ли ошибок

## Логи для проверки

При правильной работе должны быть следующие логи:

1. **При добавлении смены подгузника:**
```
📅 Планирование напоминания о смене подгузника: {familyId: 1, eventTimestamp: '...', intervalHours: 4}
⏰ Время следующей смены: ...
⏰ Время напоминания: ...
⏰ До напоминания: X минут
✅ Напоминание о смене подгузника запланировано на ... (следующая смена в ...)
```

2. **При загрузке приложения:**
```
Starting reminder processor...
Reminder processor started (checking every 60 seconds)
```

3. **Каждую минуту (если есть напоминания):**
```
✅ Processed X reminders: Y sent, Z failed
```

4. **Если напоминаний нет (каждые 5 минут):**
```
ℹ️ No reminders to process
```

## Быстрая проверка

Выполните в консоли браузера:

```javascript
// 1. Проверка напоминаний
window.debugReminders.checkScheduledReminders(1)

// 2. Проверка подписок
window.debugReminders.checkPushSubscriptions(1)

// 3. Создание тестового напоминания (на 1 минуту)
window.debugReminders.createTestReminder(1, 'diaper')

// 4. Немедленная обработка (через ~1 минуту после создания)
window.debugReminders.processRemindersNow()
```

Если тестовое напоминание работает, значит проблема в планировании или времени напоминания.

