# Тестирование системы напоминаний

## 🚀 Быстрый тест (рекомендуется)

### Вариант 1: Немедленная проверка (без ожидания)

**Шаг 1:** Откройте консоль браузера (F12)

**Шаг 2:** Проверьте систему:
```javascript
// Замените 1 на ID вашей семьи
window.debugReminders.testReminderSystem(1)
```

**Шаг 3:** Создайте тестовое напоминание для немедленной отправки:
```javascript
// Создать напоминание, которое можно отправить сразу
window.debugReminders.createImmediateTestReminder(1, 'diaper')
```

**Шаг 4:** Обработайте напоминание вручную:
```javascript
window.debugReminders.processRemindersNow()
```

**Результат:** Должно прийти push-уведомление в течение нескольких секунд!

### Вариант 2: Тест с ожиданием (реалистичный)

**Шаг 1:** Создайте тестовое напоминание на 1 минуту вперед:
```javascript
window.debugReminders.createTestReminder(1, 'diaper')
```

**Шаг 2:** Подождите 1-2 минуты (обработчик проверит автоматически)

**Результат:** Уведомление придет автоматически через 1-2 минуты

## Подробное тестирование

### Тест 1: Проверка сохранения напоминаний

1. Добавьте событие (кормление/подгузник/купание)
2. Проверьте консоль - должны быть логи планирования
3. Проверьте БД:
```javascript
window.debugReminders.checkScheduledReminders(1)
```

### Тест 2: Проверка подписок

**Критично:** Без подписок уведомления не придут!

```javascript
window.debugReminders.checkPushSubscriptions(1)
```

Если подписок нет:
1. Убедитесь, что вы подписаны на push-уведомления
2. Проверьте настройки приложения

### Тест 3: Создание тестового напоминания

```javascript
// Создать тестовое напоминание на 1 минуту вперед
window.debugReminders.createTestReminder(1, 'diaper')
```

Проверьте, что оно создалось:
```javascript
window.debugReminders.checkScheduledReminders(1)
```

### Тест 4: Ручная обработка

```javascript
// Обработать напоминания немедленно
window.debugReminders.processRemindersNow()
```

В консоли должно появиться:
```
✅ Processed 1 reminders: X sent, 0 failed
```

### Тест 5: Проверка отправки

После обработки проверьте:
1. Получили ли вы push-уведомление
2. Статус напоминания в БД (должен быть `sent`)
3. Логи в консоли

## Проверка через SQL

### Проверка запланированных напоминаний

```sql
SELECT 
    id,
    reminder_type,
    scheduled_time,
    event_time,
    status,
    NOW() as current_time,
    scheduled_time <= NOW() as is_ready
FROM scheduled_reminders 
WHERE family_id = 1
AND status = 'pending'
ORDER BY scheduled_time ASC;
```

### Проверка отправленных напоминаний

```sql
SELECT 
    id,
    reminder_type,
    scheduled_time,
    sent_at,
    sent_count
FROM scheduled_reminders 
WHERE family_id = 1
AND status = 'sent'
ORDER BY sent_at DESC
LIMIT 10;
```

### Создание тестового напоминания через SQL

```sql
-- Создать тестовое напоминание на 1 минуту вперед
INSERT INTO scheduled_reminders (
    family_id,
    reminder_type,
    scheduled_time,
    event_time,
    status
) VALUES (
    1,  -- ваш family_id
    'diaper',  -- или 'feeding', 'bath'
    NOW() + INTERVAL '1 minute',
    NOW() + INTERVAL '6 minutes',
    'pending'
);
```

## Типичный тест-кейс

### Полный тест системы

```javascript
// 1. Проверить систему
window.debugReminders.testReminderSystem(1)

// 2. Проверить подписки (должны быть!)
window.debugReminders.checkPushSubscriptions(1)

// 3. Создать тестовое напоминание
window.debugReminders.createTestReminder(1, 'diaper')

// 4. Проверить, что оно создалось
window.debugReminders.checkScheduledReminders(1)

// 5. Обработать немедленно
window.debugReminders.processRemindersNow()

// 6. Проверить результат
// - Должно прийти push-уведомление
// - В консоли должно быть: "✅ Processed 1 reminders: X sent, 0 failed"
```

## Что проверять

### ✅ Успешный тест

1. Напоминание создается в БД
2. Обработчик находит напоминание
3. Push-уведомление отправляется
4. Статус напоминания меняется на `sent`
5. В консоли есть логи об успешной отправке

### ❌ Проблемы

1. **Нет подписок** - исправьте через настройки приложения
2. **Напоминание не обрабатывается** - проверьте, что обработчик запущен
3. **Ошибка при отправке** - проверьте VAPID ключи
4. **RLS ошибка** - выполните SQL миграцию

## Проверка в продакшене

После деплоя в Vercel:

1. Проверьте, что обработчик работает (логи в консоли)
2. Создайте тестовое напоминание
3. Подождите 1-2 минуты
4. Проверьте, что уведомление пришло

## Автоматическая проверка

Обработчик автоматически проверяет напоминания каждую минуту. Если напоминание готово к отправке, оно будет обработано в течение 1 минуты.

