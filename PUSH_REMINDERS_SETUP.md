# Настройка системы Push-уведомлений с напоминаниями

## Обзор

Система push-уведомлений теперь поддерживает автоматические напоминания за 15 минут до:
- **Кормления** - на основе интервала кормления из настроек
- **Смены подгузника** - на основе интервала смены подгузника из настроек

## Что было реализовано

### 1. Автоматическое планирование напоминаний

При добавлении нового кормления или смены подгузника автоматически планируется напоминание за 15 минут до следующего события.

### 2. Отправка уведомлений всем членам семьи

Компонент `NotificationSender` позволяет отправить уведомление всем членам семьи (включая себя) или конкретным пользователям.

## Настройка

### Шаг 1: Создание таблицы в базе данных

Выполните SQL скрипт для создания таблицы `scheduled_reminders`:

```sql
-- Запустите database_scheduled_reminders.sql в Supabase SQL Editor
```

Этот скрипт создаст:
- Таблицу `scheduled_reminders` для хранения запланированных уведомлений
- Индексы для оптимизации запросов
- RLS политики для безопасности

### Шаг 2: Настройка Vercel Cron Jobs

В файле `vercel.json` уже добавлен cron job для обработки запланированных напоминаний:

```json
{
  "crons": [
    {
      "path": "/api/push/process-reminders",
      "schedule": "*/1 * * * *"
    }
  ]
}
```

Это означает, что `/api/push/process-reminders` будет вызываться каждую минуту для проверки и отправки запланированных уведомлений.

### Шаг 3: Проверка переменных окружения

Убедитесь, что в Vercel настроены следующие переменные окружения:

- `VAPID_PUBLIC_KEY` - Публичный VAPID ключ
- `VAPID_PRIVATE_KEY` - Приватный VAPID ключ
- `VAPID_SUBJECT` - Email для VAPID (например, `mailto:noreply@babycare.app`)
- `SUPABASE_URL` - URL вашего Supabase проекта
- `SUPABASE_SERVICE_ROLE_KEY` - Service Role ключ Supabase

## Использование

### Автоматические напоминания

Напоминания планируются автоматически при добавлении:
- **Кормления** - через `dataService.addFeeding()`
- **Смены подгузника** - через `dataService.addDiaper()`

Система:
1. Получает последнее событие и интервал из настроек
2. Вычисляет время следующего события
3. Планирует напоминание за 15 минут до события
4. Сохраняет напоминание в таблицу `scheduled_reminders`

### Ручная отправка уведомлений

Используйте компонент `NotificationSender` в настройках приложения:

1. Откройте раздел "Настройки"
2. Найдите секцию "📤 Отправить уведомление"
3. Выберите получателей:
   - "Всем членам семьи (включая меня)" - отправка всем
   - Или выберите конкретных пользователей
4. Введите заголовок и текст уведомления
5. Нажмите "📤 Отправить уведомление"

## API Endpoints

### POST `/api/push/schedule-reminder`

Планирует напоминание о кормлении или смене подгузника.

**Тело запроса:**
```json
{
  "familyId": 1,
  "reminderType": "feeding" | "diaper",
  "scheduledTime": "2024-01-01T12:00:00Z",
  "eventTime": "2024-01-01T12:15:00Z"
}
```

### POST `/api/push/cancel-reminders`

Отменяет запланированные напоминания.

**Тело запроса:**
```json
{
  "familyId": 1,
  "reminderType": "feeding" | "diaper"
}
```

### POST `/api/push/process-reminders`

Обрабатывает и отправляет запланированные напоминания (вызывается автоматически через cron).

**Ответ:**
```json
{
  "success": true,
  "processed": 2,
  "sent": 5,
  "failed": 0
}
```

## Структура базы данных

### Таблица `scheduled_reminders`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | SERIAL | ID напоминания |
| `family_id` | INTEGER | ID семьи |
| `reminder_type` | TEXT | Тип напоминания (`feeding` или `diaper`) |
| `scheduled_time` | TIMESTAMP | Время отправки напоминания |
| `event_time` | TIMESTAMP | Время события |
| `status` | TEXT | Статус (`pending`, `sent`, `cancelled`) |
| `sent_at` | TIMESTAMP | Время отправки |
| `sent_count` | INTEGER | Количество отправленных уведомлений |

## Локальная разработка

### Запуск обработчика напоминаний вручную

```bash
node scripts/process-scheduled-reminders.js
```

### Настройка локального cron (опционально)

Для локальной разработки можно настроить cron job:

```bash
# Linux/Mac
*/1 * * * * cd /path/to/project && node scripts/process-scheduled-reminders.js

# Windows (используя Task Scheduler)
# Создайте задачу, которая запускается каждую минуту
```

## Отладка

### Проверка запланированных напоминаний

```sql
SELECT * FROM scheduled_reminders 
WHERE status = 'pending' 
ORDER BY scheduled_time ASC;
```

### Проверка отправленных напоминаний

```sql
SELECT * FROM scheduled_reminders 
WHERE status = 'sent' 
ORDER BY sent_at DESC 
LIMIT 10;
```

### Логи

Логи обрабатываются через консоль Vercel или локальный терминал:
- Успешное планирование напоминаний
- Ошибки при планировании
- Отправка уведомлений
- Ошибки отправки

## Безопасность

- RLS политики ограничивают доступ к напоминаниям только для членов семьи
- VAPID ключи обеспечивают безопасность push-уведомлений
- Валидация всех входных данных на API endpoints

## Troubleshooting

### Напоминания не планируются

1. Проверьте, что таблица `scheduled_reminders` создана
2. Проверьте логи браузера на наличие ошибок
3. Убедитесь, что последнее событие (кормление/подгузник) существует
4. Проверьте настройки интервалов семьи

### Напоминания не отправляются

1. Проверьте, что cron job настроен в Vercel
2. Проверьте логи Vercel на наличие ошибок
3. Убедитесь, что пользователи подписаны на push-уведомления
4. Проверьте VAPID ключи в переменных окружения

### Уведомления не приходят на устройство

1. Проверьте, что пользователь подписан на уведомления
2. Проверьте разрешения браузера на уведомления
3. Убедитесь, что Service Worker зарегистрирован
4. Проверьте VAPID ключи (должны совпадать на клиенте и сервере)

## Дополнительная информация

- См. `PUSH_NOTIFICATIONS_README.md` для общей информации о push-уведомлениях
- См. `VERCEL_VAPID_SETUP.md` для настройки VAPID ключей

